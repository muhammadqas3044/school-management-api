# school-management-api
# 🏫 School Management API (FastAPI)  A simple **School Management API** built using **FastAPI**.   This project demonstrates basic CRUD operations for students and their results using **dictionary-based storage** (no database required).
